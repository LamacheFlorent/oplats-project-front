const initialState = {
    // ici le state initial
  };
  
  const recipesReducer = (state = initialState, action = {}) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default recipesReducer;